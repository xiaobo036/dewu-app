{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
  "compilerOptions": {
    "baseUrl": ".", // 基础路径（相对于 tsconfig.json 位置）
    "paths": {
      "@/*": ["src/*"] // @/* 映射到 src/*
    }
    // 其他配置...
  }
}
